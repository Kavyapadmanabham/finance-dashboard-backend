# MongoDB connection string (local example)
MONGO_URI=mongodb://127.0.0.1:27017/financeDataDatabase

# Server port
PORT=5000

# Node environment
NODE_ENV=development

# JWT configuration
JWT_SECRET=replace-with-a-strong-secret
JWT_EXPIRES_IN=1d
